/**
 * @deprecated Superseded by AppShell (header + MobileNav + LegalFooter).
 * Kept as a re-export so older imports keep working.
 */
export { AppShell as SiteShell } from "@/components/layout/app-shell";
